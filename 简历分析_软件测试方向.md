# 轻量级高并发日志数据处理系统 —— 软件测试岗位简历撰写参考

> 本文档从**软件测试工程师**视角，重新拆解本项目的技术栈、项目介绍与亮点，便于你精准投递测试类岗位（功能测试 / 接口测试 / 性能测试 / 测试开发）。

---

## 一、项目介绍（测试视角版）

**本科毕业设计，基于 Go 语言独立开发的高并发日志数据处理系统，并独立完成覆盖性能、接口、可靠性、安全的全链路测试与质量保障工作。**

---

## 二、技术栈（突出测试相关能力）

| 类别 | 技术关键词 |
|------|-----------|
| **后端被测系统** | Go 1.21 / Gin / SQLite (CGO mattn/go-sqlite3) / 异步批量 I/O / 数据保留策略 |
| **接口测试** | RESTful API / HTTP / TCP / UDP / JSON / Postman / curl |
| **性能测试** | Python 多线程压测 / ThreadPoolExecutor / QPS 测算 / 并发梯度 / 稳定性测试 |
| **测试数据构造** | Python 脚本造数 / 日志格式转换 / 边界值数据 / 异常注入 |
| **可观测与监控** | Prometheus Metrics / 服务端状态 API / 队列深度监控 / 刷盘延迟检测 |
| **质量保障** | 数据完整性校验 / 优雅关闭验证 / 背压测试 / 磁盘溢写测试 / 多格式兼容性测试 |

> **简历一句话写法**：
> `Go / Gin / SQLite / Python 压测 / 接口自动化 / 性能测试 / Prometheus / 多协议接入测试`

---

## 三、项目亮点（6条，突出一人完成的测试体系）

1. **独立设计并执行全链路性能压测，精准定位存储瓶颈**  
   针对系统 TCP/UDP/HTTP 三种接收器，使用 Python 多线程独立开发压测工具链，设计"发送-采样-排空-稳定"四步测试法，精准测算单机 sustained 吞吐 **1,500+ QPS**、突发 **20,000+ QPS**。通过梯度并发测试定位 SQLite 单线程写锁瓶颈，推动引入异步批量存储优化，最终将写入延迟从 **3-5ms 降至 <1ms**，吞吐提升 **5 倍以上**。

2. **独立开发测试工具链与自动化数据工厂**  
   独立编写 `stress_test.py`（多协议压测发送器）、`sustained_qps_test.py`（持续吞吐率测算）、`generate_test_logs.py`（造数工具）、`convert_log_format.py`（格式转换）等配套脚本，支持并发数、速率、批量大小、持续时间等参数化配置，实现一键压测与结果自动校验。

3. **完成 15+ RESTful 接口的功能与异常场景测试**  
   对系统的日志查询、异步导入、批量导出、配置热更新、接收器启停等核心 API 设计并执行黑盒测试用例，覆盖正向流程、参数边界、权限控制、错误码及并发导入进度轮询（`/api/logs/import/progress`），验证接口幂等性与数据一致性。

4. **设计可靠性测试方案，验证极端场景下的数据完整性**  
   构造内存队列满载、信号中断、运行时配置热更新等异常场景：验证磁盘溢写保护机制（`DiskOverflowQueue`）的触发阈值、回灌策略与上限控制；模拟 `SIGINT` / `SIGTERM` 触发优雅关闭，通过"发送量-入库量"比对验证**数据零丢失**；持续压测中动态调整 Worker 数，验证系统平稳过渡无 panic。

5. **执行 HTTP 接收器的安全渗透测试**  
   针对日志接收端设计并执行安全测试用例：Token 认证绕过（Header / Query 参数）、IP 白名单伪造（`X-Forwarded-For` / `X-Real-Ip` 代理场景）、单 IP 速率限制突破、超大请求体 DoS、单请求万行日志攻击等，验证四层安全防护机制的有效性。

6. **SQLite 轻量级存储方案选型与异步化性能优化**  
   选用 SQLite 作为嵌入式持久化方案（单文件、零配置、CGO 驱动），满足系统轻量部署需求。针对 SQLite 库级写锁导致的高并发写入瓶颈，独立设计 `AsyncStorage` 异步存储包装器：内存缓冲队列（5万条）+ 批量写入（1,000条/批）+ 定时 Flush（100ms），将写入延迟从 **3-5ms 降至 <1ms**，sustained 吞吐提升 **5 倍以上**。同时实现数据保留策略（`retention_hours`）自动清理过期数据、在线 `VACUUM` 压缩接口回收空间，有效控制磁盘占用。

---

## 四、简历 Bullet Points 示例（直接复制可用）

> 以下按“性能 + 工具 + 接口 + 可靠性 + 安全 + 可观测”六维组合，可直接放入简历项目经历。

**方案 A：偏性能测试 / 测开方向（推荐）**

- 本科毕设，独立完成 Go 语言日志处理系统的开发与全链路测试；设计"发送-采样-排空-稳定"四步性能测试法，精准测算单机 sustained 吞吐 **1,500+ QPS**、突发 **20,000+ QPS**，定位 SQLite 写锁瓶颈并推动异步存储优化，写入延迟降至 **<1ms**。
- 独立开发 Python 多协议压测工具链（TCP/UDP/HTTP）与测试数据工厂，支持并发、速率、批量大小等参数化配置，实现一键压测与自动结果校验。
- 完成系统 15+ RESTful 接口的功能与异常测试，覆盖日志查询、异步导入进度轮询、批量导出等核心流程，验证接口幂等性与数据一致性。
- 设计可靠性测试方案，验证背压溢写保护、优雅关闭数据完整性、配置热更新稳定性，确保极端场景下数据零丢失。
- 执行 HTTP 接收器安全渗透测试（认证绕过、IP 伪造、速率限制突破、DoS 攻击），验证四层安全防护有效性。
- 选用 SQLite 作为嵌入式持久化存储（CGO 驱动），设计 `AsyncStorage` 异步包装器解决单线程写锁瓶颈：内存缓冲队列 + 批量写入 + 定时 Flush，写入延迟降至 **<1ms**，吞吐提升 **5 倍**；实现数据保留策略与 `VACUUM` 在线压缩，控制磁盘占用。

**方案 B：偏功能 / 接口测试方向**

- 本科毕设，独立开发 Go 语言日志处理系统并承担全部测试工作；对 15+ 核心 API 设计黑盒测试用例，覆盖正向流程、参数边界、权限控制及错误码验证。
- 针对大文件异步导入场景，设计"上传-轮询进度-最终校验"闭环测试流程，验证异步进度接口的实时性与数据一致性。
- 独立完成多协议性能压测，使用 Python 开发压测发送器与持续吞吐率测算工具，建立系统性能基线。
- 构造内存队列满载、信号中断等异常场景，验证磁盘溢写保护与优雅关闭机制，确保数据不丢失。
- 对 HTTP 日志接收端执行 Token 绕过、IP 伪造、请求体超限等安全测试，发现并推动修复潜在 DoS 风险。
- 选用 SQLite 作为轻量级存储方案，针对高并发写入设计异步批量写入机制（缓冲队列 + 批量 Flush），解决库级写锁瓶颈；实现数据保留自动清理与 `VACUUM` 压缩功能，保障长时间运行下的磁盘可控。

**方案 C：偏测试开发 / 自动化方向**

- 本科毕设，独立完成日志处理系统开发，并配套建设完整的测试工具体系；使用 Python 开发多协议压测发送器、持续吞吐率测算工具、造数与格式转换脚本，支持参数化配置与自动化结果比对。
- 设计并执行并发扩展性、文件导入压力、 sustained QPS 稳定性等多维度性能测试，建立含峰值 QPS / 稳定期 QPS / 尾延迟的标准化基线报告。
- 完成 15+ RESTful 接口的自动化测试设计，覆盖日志查询、导入、导出、配置管理等模块，具备 CI 接入能力。
- 推动系统可测试性建设：利用 `/api/status`、`/metrics`、导入进度查询等接口实现白盒监控，快速定位性能瓶颈与异常根因。
- 设计可靠性测试方案，验证队列背压、磁盘溢写回灌、优雅关闭数据完整性等极端场景。
- 执行 HTTP 接收器安全渗透测试，覆盖认证、IP 白名单、速率限制、请求体上限等四层防护机制；选用 SQLite 实现轻量级持久化，通过异步批量写入与数据保留策略优化存储性能与磁盘占用。

---

## 五、面试时可展开的技术细节（加分储备）

| 话题 | 你可以这样讲 |
|------|-------------|
| **你怎么测性能？** | 讲“四步测试法”（发送→每秒采样服务端入库数→发送结束后轮询队列排空→稳定后算 Sustained QPS），强调排除尾延迟，数据更真实。 |
| **怎么验证数据没丢？** | 压测前清空服务端日志并记录 `before`，压测后等待队列排空记录 `after`，计算 `stored_added / sent` 的入库率；优雅关闭场景同样用此法验证。 |
| **怎么构造测试数据？** | 用 `generate_test_logs.py` 按格式/量级生成；用 `convert_log_format.py` 做格式转换；手动构造超大字段、非法 JSON、特殊分隔符等异常数据。 |
| **异步导入怎么测？** | 上传后轮询 `/api/logs/import/progress`，对比 `ParsedLines` 和 `WrittenLines` 是否最终一致；测试中断重试后的幂等性。 |
| **配置热更新测试难点？** | 难点在于“运行时动态扩缩容 Worker 协程”不能丢数据或 panic。测试方法：持续压测中调用 API 改配置，观察 QPS 曲线平稳过渡，无错误日志。 |
| **安全测试做了哪些？** | HTTP 接收器的四层防护：Token（Header/Query）、IP 白名单（含 X-Forwarded-For 代理场景）、单 IP 速率限制（1分钟滑动窗口）、请求体/行数上限。 |
| **SQLite 存储层怎么设计和优化的？** | 选型：SQLite 单文件零配置，适合轻量级部署。瓶颈：库级写锁导致高并发下串行写入。优化：设计 `AsyncStorage` 包装器，内存缓冲（5万条）+ 批量写入（1000条/批）+ 定时 Flush（100ms），延迟从 3-5ms 降至 <1ms。运维：配置 `retention_hours` 自动清理，提供 `VACUUM` 接口手动回收空间。 |
| **发现过什么 bug？** | 示例：早期同步存储模式下，高并发时 SQLite 单线程写锁导致写入延迟 3-5ms，压测发现 QPS 瓶颈；推动引入 `AsyncStorage` 异步批量写入后，延迟降至 <1ms，吞吐提升 5 倍。 |

---

## 六、写在最后

作为测试工程师，写简历的核心是：**你不是在“介绍项目”，而是在“证明你能保障质量”**。  
本项目是一个非常好的载体——它是你的毕设，从系统开发到测试验证都由你一手完成，有丰富的接口、明确的性能指标、完整的异常容错机制，以及你自己写的测试工具和脚本。把这些用**量化数据**和**测试方法论**表达出来，会比单纯罗列功能点更有说服力。

祝你面试顺利！
