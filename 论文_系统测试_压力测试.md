# 系统测试

## 5.3.2 压力测试

压力测试旨在验证日志处理系统在高并发、大数据量场景下的稳定性、吞吐量及性能瓶颈，重点考察实时数据接收链路与文件导入链路的极限处理能力。

### （1）测试环境

本次压力测试在单机环境下进行，硬件与软件配置如表 5-1 所示。

**表 5-1 压力测试环境配置**

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows 11 |
| CPU | Intel Core i7-12700H |
| 内存 | 16 GB DDR5 |
| 硬盘 | NVMe SSD 512 GB |
| 开发语言 | Go 1.21 |
| 数据库 | SQLite 3 |
| 网络 | 本地回环（127.0.0.1）|

系统采用异步存储架构（v2.0），核心参数配置为：内存缓冲队列 50,000 条、批量写入大小 1,000 条/批次、后台刷新间隔 100 ms、SQLite 后台写入协程 1 个。

### （2）测试内容与方法

压力测试覆盖系统的两条核心数据入口：**实时流式导入**与**批量文件导入**。测试脚本基于 Python 编写，通过独立的发送端模拟高并发客户端行为，服务端与发送端运行在同一台物理机上，以排除网络抖动对结果的干扰。

#### 1）实时导入最大压力测试

实时导入测试使用 `stress_test.py` 压测工具，分别针对 TCP、UDP、HTTP 三种接收协议进行高并发日志注入。测试通过多线程/多协程并发发送模拟日志源，支持按固定总量（如 20,000 条）或持续时间（如 30 s）两种模式运行。关键测试场景如下：

- **TCP 单连接持续压测**：100 并发连接向 `:9000` 端口持续发送 Nginx 格式日志，总发送量 100,000 条；
- **UDP 无连接突发压测**：100 并发线程向 `:9001` 端口以最高速率发送日志，考察系统对无状态突发的承载能力；
- **HTTP 批量写入压测**：通过 `POST /logs` 接口以批量方式（batch=100）向 `:9002` 端口发送日志，验证 HTTP 接收器在批量请求下的吞吐表现。

测试指标包括：**客户端发送速率（QPS）**、**服务端实际入库数量**、**入库成功率**、**平均响应延迟**。每次测试前通过 `DELETE /api/logs` 清空服务端数据，测试结束后等待后端刷盘完成，再通过 `GET /api/logs?limit=1` 校验入库总数。典型压测命令如下：

```bash
# TCP 持续压测：20 并发、总量 20 000 条、每秒 40 条
python stress_test.py -protocol tcp -addr localhost:9000 -total 20000 -c 20 -rate 40

# UDP 突发压测：50 并发、总量 50 000 条、不限速
python stress_test.py -protocol udp -addr localhost:9001 -total 50000 -c 50

# HTTP 批量压测：10 并发、批量 100 条、总量 50 000 条
python stress_test.py -protocol http -addr localhost:9002 -total 50000 -c 10 -batch 100
```

#### 2）文件导入最大压力测试

文件导入测试使用 `file_import_stress_test.py` 工具，通过 Web API `POST /api/logs/import` 并发上传大体积日志文件，测试离线导入链路的极限吞吐与稳定性。该链路绕过实时处理队列与异步存储缓冲，采用批量直连写入方式。测试场景如下：

- **单大文件导入**：生成 50,000 行 Nginx 格式日志文件（约 6 MB），调用文件导入接口，检测解析、写入与进度反馈的完整性；
- **多文件并发导入**：5 个并发线程同时上传各自独立的 50,000 行日志文件，验证服务端对并发文件上传的承载能力与资源隔离效果；
- **进度监控校验**：通过 `GET /api/logs/import/progress?id={import_id}` 实时读取导入进度，验证进度状态转换（preparing → processing → completed）的准确性。

文件导入压测命令如下：

```bash
# 单大文件导入：生成 50 000 行日志文件并上传
python file_import_stress_test.py -lines 50000 -files 1 -clear

# 多文件并发导入：5 个并发线程同时上传 50 000 行日志文件
python file_import_stress_test.py -lines 50000 -files 5 -clear
```

### （3）测试结果与分析

经过多轮压力测试，系统在处理能力与资源占用方面表现稳定，核心指标如表 5-2 所示。

**表 5-2 压力测试核心指标结果**

| 测试项 | 指标 | 测试结果 |
|--------|------|----------|
| 实时导入（TCP） | 持续吞吐量 | ≥ 1,500 QPS |
| 实时导入（TCP/UDP） | 突发吞吐量 | ≥ 20,000 QPS |
| 实时导入（HTTP batch） | 平均写入延迟 | < 1 ms |
| 实时导入 | 入库成功率 | ≥ 95 % |
| 文件导入 | 单文件 5 万行 | 平均 8–12 s 完成 |
| 文件导入 | 5 并发大文件 | 导入吞吐稳定，无 OOM |
| 系统资源 | 内存占用 | < 200 MB |
| 系统资源 | 并发连接/线程 | 1,000+ |

**结果分析：**

1. **异步存储架构有效消除了 I/O 瓶颈**。在同步模式下，SQLite 的阻塞写入将系统吞吐量限制在约 300 QPS；引入异步队列与批量后台写入后，持续吞吐量提升至 1,500+ QPS，突发场景下可达 20,000+ QPS，增幅约 5–7 倍。

2. **实时接收器协议差异明显**。TCP 与 UDP 接收器由于省去了 HTTP 协议栈开销，在同等并发数下表现最优；HTTP 接收器在批量请求（batch=100）模式下，通过减少连接与解析开销，也能达到较高的吞吐水平。三种协议在测试期间均未出现连接泄露或服务崩溃。

3. **文件导入链路具备高可靠性**。离线导入绕过内存队列，直接通过批量事务写入 SQLite，5 并发 × 5 万行场景下系统内存占用始终低于 200 MB，未触发溢出队列（overflow queue），进度接口反馈准确，格式不匹配等异常场景亦能正确返回 warning 状态。

4. **系统存在明确的饱和点**。当 TCP 发送端速率超过约 2,500 QPS 并持续运行时，由于 SQLite 后台刷盘速率存在上限，部分日志开始暂存于溢出队列或触发丢弃；降低发送速率至 1,500 QPS 以下后，系统可长期稳定运行，丢包率趋近于 0。

综上，压力测试表明本系统在高并发日志接收与批量文件导入场景下均具备较强的处理能力与稳定性，能够满足中等规模业务场景的实时数据处理需求。
